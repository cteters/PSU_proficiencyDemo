#include "CS299_bst.h"
//Please place your name here:
//
//

int main()
{
    int num = 0;

    table object;
    object.build(); 	//builds a BST
    object.display();	//displays a BST

    /*  PLACE YOUR FUNCTION CALL HERE */
/*    cout << "The largest value in the tree is ";
    cout << object.remove_largest();
    cout << " and is now removed." << endl << endl;
*/

/*    cout << "All values smaller than root:" << endl;
    num = object.display_smaller_root();
    cout << "For a total of " << num << " nodes." << endl << endl;
*/

/*    node * cpy = NULL;
    cout << "making a copy of each node divisible by 2... " <<endl;
    num = object.copy_div_two(cpy);
    cout << num << " nodes were copied." << endl << endl;
    display_nodes(cpy);
    cout << endl;
    dealocate_nodes(cpy);
*/

    
    


    object.display();	//displays again after!
   
    return 0;
}
